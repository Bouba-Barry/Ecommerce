<form method="post" action="{{ path('app_fead_back_delete_get', { id: feed_back.id }) }}" onsubmit="return confirm('vous voulez  le supprimer vraiment ??');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ feed_back.id) }}"/>
	<button class="btn">supprimer</button>
</form>
