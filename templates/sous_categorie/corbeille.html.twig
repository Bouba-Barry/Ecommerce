{% extends 'admin.html.twig' %} {% block stylesheet %} {% endblock stylesheet %}
{# {% block title %}Produit index{% endblock %} #} {% block content %}

<!-- Content Wrapper. Contains page content -->
<div class="content-wrapper">
  <div class="container-full">
    <!-- Content Header (Page header) -->
    <div class="content-header">
      <div class="d-flex align-items-center">
        <div class="mr-auto">
          <h3 class="page-title">Liste Sous Categories supprimes</h3>
          <div class="d-inline-block align-items-center">
            <ol class="breadcrumb">
              <li>
                <a
                  href="{{ path('app_sous_categorie_index') }}"
                  class="btn btn-primary btn-md"
                  >Liste Sous Categories</a
                >
              </li>
            </ol>
          </div>
        </div>
      </div>
    </div>

    <!-- Main content -->
    <section class="content">
      <div class="row">
        <div class="col-12">
          <div class="box">
            <!-- /.box-header -->
            <div class="box-body">
              <div class="table-responsive">
                <table
                  id="example5"
                  class="table table-bordered table-striped"
                  style="width: 100%"
                >
                  <thead>
                    <tr>
                      <th col-1>ID</th>
                      <th>Nom De la Sous Categorie</th>
                      <th>Action</th>
                    </tr>
                  </thead>
                  <tbody>
                    {% for sous_categorie in sous_categories %}
                    <tr>
                      <td class="">{{ sous_categorie.id }}</td>
                      <td>{{ sous_categorie.titre }}</td>
                      <td>
                        <a
                          href="{{
                            path('app_sous_categorie_restore', {
                              id: sous_categorie.id
                            })
                          }}"
                          class="btn btn-md btn-success"
                        >
                          Restore
                          <i class="fa-solid fa-trash-can-arrow-up"></i>
                        </a>
                        <a
                          href="{{
                            path('app_sous_categorie_delete_fromcorbeille', {
                              id: sous_categorie.id
                            })
                          }}"
                          class="btn btn-md btn-danger"
                        >
                          delete from trash
                          <i class="fa fa-trash"></i>
                        </a>
                        {#
                        {{ include("sous_categorie/_delete_form.html.twig") }}
                        #}
                      </td>
                    </tr>
                    {% else %}
                    <tr>
                      <td colspan="9">no records found</td>
                    </tr>
                    {% endfor %}
                  </tbody>
                  <tfoot>
                    <tr>
                      <th>ID</th>
                      <th style="border-right: 1px solid #d5dfea">
                        Nom Sous Categorie
                      </th>
                    </tr>
                  </tfoot>
                </table>
              </div>
            </div>
            <!-- /.box-body -->
          </div>
          <!-- /.box -->
        </div>
        <!-- /.col -->
      </div>
      <!-- /.row -->
    </section>
    <!-- /.content -->
  </div>
</div>
<!-- /.content-wrapper -->

{% endblock %} {% block javascript %}
<script src="{{
    asset('assets/vendor_components/datatable/datatables.min.js')
  }} "></script>
<script src="{{ asset('main/js/pages/data-table.js') }}"></script>
{% endblock %}
