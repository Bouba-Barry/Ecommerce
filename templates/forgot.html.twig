<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content="" />
    <meta name="author" content="" />
    <link rel="icon" href="{{ asset('images/favicon.ico') }}" />

    <title>Recuperation du Mot de Passe</title>

    <!-- Vendors Style-->
    <link rel="stylesheet" href="{{ asset('main/css/vendors_css.css') }} " />

    <!-- Style-->
    <link rel="stylesheet" href="{{ asset('main/css/style.css') }} " />
    <link rel="stylesheet" href="{{ asset('main/css/skin_color.css') }}" />
  </head>

  <body class="hold-transition theme-primary bg-gradient-primary">
    <div class="container h-p100">
      <div class="row align-items-center justify-content-md-center h-p100">
        <div class="col-12">
          <div class="row justify-content-center no-gutters">
            <div class="col-lg-4 col-md-5 col-12">
              <div class="content-top-agile p-10">
                <h3 class="mb-0 text-white">Réinitialiser le Mot de Passe</h3>
              </div>
              <div class="p-30 rounded30 box-shadowed b-2 b-dashed">
                <div class="form-group">
                  <div class="input-group mb-3">
                    <div class="input-group-prepend">
                      <span class="input-group-text bg-transparent text-white"
                        ><i class="ti-email"></i
                      ></span>
                    </div>
                    <input
                      name="email"
                      id="email"
                      type="email"
                      class="form-control pl-15 bg-transparent text-white plc-white"
                      placeholder="Email"
                    />
                  </div>
                </div>
                <div class="form-group" id="form-group" style="display: none">
                  {#
                  <h3 class="text-white">Entrer le code reçue par email</h3>
                  #}
                  <label class="text-white" for="code"
                    >Entrer le code reçue par email</label
                  >
                  <div class="input-group mb-3">
                    <div class="input-group-prepend">
                      <span class="input-group-text bg-transparent text-white"
                        ><i class="ti-email"></i
                      ></span>
                    </div>

                    <input
                      name="code"
                      id="code"
                      type="number"
                      class="form-control pl-15 bg-transparent text-white plc-white"
                      placeholder=" code reçue "
                    />
                  </div>
                  <p class="text-red" id="text" style="display: none"></p>
                </div>

                <div class="row">
                  <div class="col-12 text-center">
                    <button
                      id="submit"
                      class="btn btn-info btn-rounded margin-top-10"
                    >
                      Réinitialiser
                    </button>
                  </div>
                  <!-- /.col -->
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Vendor JS -->
    <script src="{{ asset('main/js/vendors.min.js') }} "></script>
    <script src="{{
        asset('assets/icons/feather-icons/feather.min.js')
      }} "></script>

    <script src="{{ asset('main/js/forgot/forgot.js') }}"></script>
  </body>
</html>
