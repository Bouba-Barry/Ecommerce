
  {{ include('attribut/_form.html.twig', {'button_label': 'Update'}, {'action': path("app_attribut_edit", {'id': attribut.id})}) }}

    <a href="{{ path('app_attribut_index') }}">back to list</a>

    {{ include('attribut/_delete_form.html.twig') }}
