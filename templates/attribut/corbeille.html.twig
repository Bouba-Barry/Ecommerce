{% extends 'admin.html.twig' %} {% block stylesheet %} {% endblock stylesheet %}
{# {% block title %}Produit index{% endblock %} #} {% block content %}

<!-- Content Wrapper. Contains page content -->
<div class="content-wrapper">
  <div class="container-full">
    <div id="message">
      {% for message in app.flashes('success') %}
      <div class="alert alert-success">
        {{ message }}
      </div>
      {% endfor %} {% for message in app.flashes('suppression') %}
      <div class="alert btn-danger">
        {{ message }}
      </div>
      {% endfor %}
    </div>
    <!-- Content Header (Page header) -->
    <div class="content-header">
      <div class="d-flex align-items-center">
        <div class="mr-auto">
          <h3 class="page-title">Liste Attributs Supprimes</h3>
          <div class="d-inline-block align-items-center">
            <ol class="breadcrumb">
              <li>
                <a
                  href="{{ path('app_attribut_index_aside') }}"
                  class="btn btn-primary btn-md"
                  >Tous les Attributs</a
                >
              </li>
            </ol>
          </div>
        </div>
      </div>
    </div>

    <!-- Main content -->
    <section class="content">
      <div class="row">
        <div class="col-12">
          <div class="box">
            <!-- /.box-header -->
            <div class="box-body">
              <div class="table-responsive">
                <table
                  id="example5"
                  class="table table-bordered table-striped"
                  style="width: 100%"
                >
                  <thead>
                    <tr>
                      <th col-1>ID</th>
                      <th>Désignation</th>
                      <th>Action</th>
                    </tr>
                  </thead>
                  <tbody>
                    {% for attribut in attributs %}
                    <tr>
                      <td class="">{{ attribut.id }}</td>
                      <td>{{ attribut.nom }}</td>
                      <td>
                        <a
                          href="{{
                            path('app_attribut_restore', { id: attribut.id })
                          }}"
                          class="btn btn-md btn-success"
                        >
                          Restorer
                          <i class="fa-solid fa-trash-can-arrow-up"></i>
                        </a>
                        <a
                          href="{{
                            path('app_attribut_delete_fromcorbeille', {
                              id: attribut.id
                            })
                          }}"
                          class="btn btn-md btn-danger"
                        >
                          supprimer définitivement
                          <i class="fa fa-trash"></i>
                        </a>
                      </td>
                    </tr>
                    {% else %}
                    <tr>
                      <td colspan="9">no records found</td>
                    </tr>
                    {% endfor %}
                  </tbody>
                  <tfoot>
                    <tr>
                      <th>ID</th>
                      <th style="border-right: 1px solid #d5dfea">
                        Désignation
                      </th>
                    </tr>
                  </tfoot>
                </table>
              </div>
            </div>
            <!-- /.box-body -->
          </div>
          <!-- /.box -->
        </div>
        <!-- /.col -->
      </div>
      <!-- /.row -->
    </section>
    <!-- /.content -->
  </div>
</div>
<!-- /.content-wrapper -->

{% endblock %} {% block javascript %}
<script src="{{
    asset('assets/vendor_components/datatable/datatables.min.js')
  }} "></script>
<script src="{{ asset('main/js/pages/data-table.js') }}"></script>
{% endblock %}
