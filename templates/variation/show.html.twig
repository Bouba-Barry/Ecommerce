{% extends 'base.html.twig' %}

{% block title %}Variation{% endblock %}

{% block body %}
    <h1>Variation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ variation.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ variation.nom }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ variation.code }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_variation_index') }}">back to list</a>

    <a href="{{ path('app_variation_edit', {'id': variation.id}) }}">edit</a>

    {{ include('variation/_delete_form.html.twig') }}
{% endblock %}
